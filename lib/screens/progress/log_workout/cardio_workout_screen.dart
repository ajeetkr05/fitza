import 'package:flutter/material.dart';

class CardioWorkoutScreen extends StatelessWidget {
  const CardioWorkoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cardio Workout'),
      ),
      body: const Center(
        child: Text('Cardio screen will be built later.'),
      ),
    );
  }
}